{
  "name": "greenhouse-controller-monorepo",
  "version": "1.0.0",
  "description": "Monorepo for greenhouse controller backend and frontends",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "start:backend": "npm start --workspace=apps/backend",
    "start:react": "npm start --workspace=apps/react-app",
    "start:nextjs": "npm run dev --workspace=apps/nextjs-app"
  }
}